{
  "name": "failing-test-fixture",
  "version": "1.0.0",
  "private": true,
  "description": "Gate fixture (tests/fixtures/failing-test): Node/non-Go variant with a deliberately failing test.",
  "type": "module",
  "scripts": {
    "test": "node --test"
  }
}
